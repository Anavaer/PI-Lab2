extends layout

block content
  h1= recipe.title
  h5 Written by #{author}
  p= recipe.body
  hr
  if user
    if user.role == 'admin'
      a.btn.btn-default(href='/recipes/edit/'+recipe._id) Edit
      a.btn.btn-danger.delete-article(href='#',data-id=recipe._id) Delete
  
